import React from 'react';

const Reports = ({ match }) => {
  const kmom = match.params.kmom;


  return (

    <main>
      <h2>test {kmom} </h2>
      test
    </main>
  );
};

export default Reports;